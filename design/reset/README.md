# design/reset — the reset's working papers

| file | what |
|---|---|
| `../THE-RESET.md` | the diagnosis, the six pillars, the phases (2026-09-07) |
| `THE-GATE.md` | the loop, exactly: cold player, six critics, record, fix, again |
| `SHARED-BRIEF.md` | the brief every pillar engineer read; the API contracts between pillars |
| `CODE-MAP.md` | the codebase by subsystem with file:line pointers (pre-reset line numbers; grep) |
| `COLD-PLAYER-PROMPT.md` | the cold player's entire prompt — give it nothing else |
| `CRITIC-PROMPT.md` | the harsh critic's entire prompt — give it nothing else |
| `reports/` | one report per pillar from its engineer (VOICE, SCALE so far) |
| `rounds/` | the gate's rounds: the cold player's report and the six verdicts, per round |

The top-level `PROMPT.md` carries the owner's standing order verbatim, the
state table, and the next session's one item. `GAME.md` is the game.
`CHANGELOG.md` is the notes and the before/after.
